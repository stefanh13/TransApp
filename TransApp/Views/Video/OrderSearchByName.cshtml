@model PagedList.IPagedList<TransApp.Models.Video>
@using PagedList.Mvc;

@{
    ViewBag.Title = "OrderSearchByName";
}

@{Html.RenderPartial("Search");}

Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount

@Html.PagedListPager(Model, page => Url.Action("OrderSearchByName", new { page, currentFilter = ViewBag.CurrentFilter }))

