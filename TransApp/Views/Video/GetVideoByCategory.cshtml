@model PagedList.IPagedList<TransApp.Models.Video>
@using PagedList.Mvc;

@{
    ViewBag.Title = "GetVideoByCategory";
}

<h2>GetVideoByCategory</h2>

@{Html.RenderPartial("VideoPartialView");}

Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount

@Html.PagedListPager(Model, page => Url.Action("GetVideoByCategory", new { page }))

